const express = require('express');
var app = express();
const route = express.Router();
var tushar =require('./facebook');

var tushar =require('./facebook');

tushar.account.read([tushar.AdAccount.Fields.name, tushar.AdAccount.Fields.age])
  .then((account) => {
    logPassedTest(test1 + ':Pass', account);

const campaignId = '23843467726730655';
new Campaign(campaignId, {
  [tushar.Campaign.Fields.id]: tushar.campaign.id,
  [tushar.Campaign.Fields.name]: 'Campaign - Updated' })
  .update();
  })
  .catch((error) => {
  });

  module.exports=route;
